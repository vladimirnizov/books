import { TitlePipePipe } from './title-pipe.pipe';

describe('WordFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
